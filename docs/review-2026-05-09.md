# 实现审查报告 — 2026-05-09

## 总体评估

项目整体进度约 **60%**。核心架构（托盘、设置、API客户端、主题切换）代码已基本完成，但存在 3 个阻塞性 bug 阻断了功能通路。大量代码以未跟踪文件形式存在，尚未提交。

## 各阶段详细状态

### Phase 1 — 托盘图标与窗口生命周期

**进度: 85%**

| 文件 | 状态 | 说明 |
|------|------|------|
| `Services/TrayIconService.cs` | ✅ | 托盘创建、左键弹出/收起、右键菜单（刷新/设置/关于/退出） |
| `App.xaml.cs` | ✅ | 移除 StartupUri，OnStartup 初始化 MainWindow + TrayIconService，OnExplicitShutdown |
| `App.xaml` | ✅ | 移除 StartupUri |
| `Helpers/WindowPositionHelper.cs` | ✅ | 任务栏四方向检测、DPI 缩放、边界避让 |
| `Views/MainWindow.xaml.cs` | ✅ | ShowNearTrayIcon()、Deactivated 失焦隐藏、CloseButton→Hide() |
| `.ico 托盘图标文件` | ❌ | 缺失，改用代码动态生成（颜色 #B5A89A） |

**待办**：
- 创建正式的 16×16 .ico 文件
- ShowNearTrayIcon 中触发 PopupFadeIn 动画

---

### Phase 2 — 设置窗口与安全存储

**进度: 80%**

| 文件 | 状态 | 说明 |
|------|------|------|
| `Views/SettingsWindow.xaml` | ✅ | API Key 三组密码框、刷新间隔下拉、复选框、主题选择 |
| `Views/SettingsWindow.xaml.cs` | ✅ | PasswordBox PasswordChanged 事件处理 |
| `ViewModels/SettingsViewModel.cs` | ✅ | 加载/保存设置、按平台保存 Key、取消 |
| `Services/SecureStorageService.cs` | ✅ | DPAPI Protect/Unprotect，%AppData% 存储 |
| `Services/SettingsService.cs` | ✅ | AppSettings JSON 读写，%AppData% 存储 |
| `Services/ISettingsService.cs` | ⚠️ | 仅有主题相关方法，SettingsService 未实现此接口 |

**Bug 1** — 配置双写（`autoHideOnLostFocus` 不生效）

- `MainWindow.LoadSettings()` → 读 `Config/AppSettings.json`（项目内置文件）
- `SettingsService` → 读写 `%AppData%/TokenUsageMonitor/appsettings.json`
- 用户在设置窗口更改后，主窗口读不到新值

**待办**：
- 统一配置读取路径，MainWindow 改用 SettingsService
- SettingsService 实现 ISettingsService 或删除接口

---

### Phase 3 — API 集成（三平台）

**进度: 80%**

| 文件 | 状态 | 说明 |
|------|------|------|
| `Models/QuotaInfo.cs` | ✅ | 与需求文档数据模型对齐 |
| `Services/IApiClient.cs` | ✅ | GetQuotaAsync(apiKey, ct) 接口 |
| `Services/DeepSeekApiClient.cs` | ✅ | 真实 HTTP、JSON 解析、超时/异常处理 |
| `Services/GlmApiClient.cs` | ✅ | 同上结构 |
| `Services/KimiApiClient.cs` | ✅ | 同上结构 |
| `Services/QuotaRefreshService.cs` | ✅ | Task.WhenAll 并发、10s 超时 |
| `ViewModels/MainViewModel.cs` | ⚠️ | Refresh 已调用真实 API，MapQuotaInfosToCollections 完成 |

**Bug 2** — `_apiKeys` 永不为空，API 通路断连

```csharp
// MainViewModel.cs:55
private readonly Dictionary<string, string> _apiKeys = new();
// 从未从 SecureStorageService 加载 Key → Refresh() 直接退回到 LoadMockData()
```

**Bug 3** — DeepSeekApiClient 中 Stopwatch 创建了但结果未使用

**待办**：
- MainViewModel 构造时从 SecureStorageService 加载 _apiKeys
- 或者通过 TrayIconService/App 启动时注入
- MapGlmData/KimiData 当前只映射一个卡片，需扩展为完整的三卡片/多模型列表
- 图表数据（ChartData/HealthData）在真实数据模式下仍用 Random，需替换

---

### Phase 4 — 并发测试

**进度: 60%**

| 文件 | 状态 | 说明 |
|------|------|------|
| `Models/ConcurrentTestResult.cs` | ✅ | PlatformName / Success / LatencyMs / ErrorMessage |
| `ViewModels/MainViewModel.cs` | ✅ | ConcurrentTestCommand 完整实现 |

**待办**：
- MainWindow.xaml 底部添加"并发测试"按钮（需求 F-109），绑定 ConcurrentTestCommand
- 测试结果展示 UI（✓ 234ms / ✗ 超时）
- ConcurrentTestCommand 中移除不必要的 `Task.Run` 包装（API 调用已异步）

---

### Phase 5 — 动画打磨

**进度: 40%**

| 文件 | 状态 | 说明 |
|------|------|------|
| `Assets/Themes/MorandiTheme.xaml` | ✅ | PopupFadeIn/PopupFadeOut Storyboard、LoadingSpinner、RefreshPulse DataTrigger |
| `Assets/Themes/MorandiDarkTheme.xaml` | ✅ | 同上，深色版本 |
| `Converters/AnimatedWidthConverter.cs` | ✅ | SmoothWidth 附加属性，300ms CubicEase 动画 |

**待办**：
- ShowNearTrayIcon 触发 PopupFadeIn
- Hide 时触发 PopupFadeOut
- 进度条 Rectangle 改用 AnimatedWidthConverter.SmoothWidth 替代静态 Width
- Loading 态 UI 接入（按钮旋转 + 卡片 Loading 状态）

---

### Phase 6 — 夜间模式

**进度: 85%**

| 文件 | 状态 | 说明 |
|------|------|------|
| `Assets/Themes/MorandiDarkTheme.xaml` | ✅ | 完整深色主题，所有颜色/Style/Storyboard |
| `Services/ThemeManager.cs` | ⚠️ | 改用 ResourceDictionary 整体切换（正确方案） |

**Bug 4** — 主题名不匹配

```
ThemeManager.ApplyTheme   →  "Light" / "Dark"
SettingsViewModel         →  "morandi_light" / "morandi_dark"
```

**Bug 5** — TryPersistTheme 通过反射调用 SettingsService 上不存在的方法

**待办**：
- 统一主题命名（建议用 "Light"/"Dark"）
- 删除反射，ThemeManager 直接调用 SettingsService.Instance
- 设置窗口主题下拉与 ThemeManager 对齐

---

### Phase 7 — 图表精细化

**进度: 0%**

未开始。图表仍使用 `Random.Shared` 生成占位数据。

---

## 阻断性 Bug 汇总

| # | 严重度 | 位置 | 问题 | 修复方案 |
|---|--------|------|------|----------|
| 1 | 🔴 阻断 | MainViewModel.cs:55 | `_apiKeys` 永不为空，API 通路断连 | 构造时从 SecureStorageService 加载 |
| 2 | 🔴 阻断 | MainWindow.xaml.cs:29 / SettingsService.cs | 配置读写路径不一致 | MainWindow 改用 SettingsService |
| 3 | 🟡 警告 | ThemeManager.cs / SettingsViewModel.cs | 主题名 "Light"/"Dark" vs "morandi_light"/"morandi_dark" | 统一为 "Light"/"Dark" |
| 4 | 🟡 警告 | ThemeManager.cs:66-93 | TryPersistTheme 反射调用不存在的方法 | 直接调用 SettingsService.Instance |

## 未跟踪文件清单

```
新文件 (21):
  Assets/Themes/MorandiDarkTheme.xaml
  Converters/AnimatedWidthConverter.cs
  Helpers/WindowPositionHelper.cs
  Models/ConcurrentTestResult.cs
  Models/QuotaInfo.cs
  Services/DeepSeekApiClient.cs
  Services/GlmApiClient.cs
  Services/IApiClient.cs
  Services/ISettingsService.cs
  Services/KimiApiClient.cs
  Services/QuotaRefreshService.cs
  Services/SecureStorageService.cs
  Services/SettingsService.cs
  Services/TrayIconService.cs
  ViewModels/SettingsViewModel.cs
  Views/SettingsWindow.xaml
  Views/SettingsWindow.xaml.cs

已修改 (7):
  App.xaml
  App.xaml.cs
  Assets/Themes/MorandiTheme.xaml
  Services/ThemeManager.cs
  ViewModels/MainViewModel.cs
  Views/MainWindow.xaml
  Views/MainWindow.xaml.cs
```

## 下一步行动

1. 修复 4 个 bug（预估 30 分钟）
2. 添加并发测试按钮 UI（预估 15 分钟）
3. 提交全部代码（28 个文件）
4. 首次启动引导流程（无 Key → 弹出设置）
5. 图表真实数据接入
